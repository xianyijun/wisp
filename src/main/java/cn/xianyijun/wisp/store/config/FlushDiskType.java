package cn.xianyijun.wisp.store.config;

/**
 * The enum Flush disk type.
 */
public enum FlushDiskType {
    /**
     * Sync flush flush disk type.
     */
    SYNC_FLUSH,
    /**
     * Async flush flush disk type.
     */
    ASYNC_FLUSH
}
